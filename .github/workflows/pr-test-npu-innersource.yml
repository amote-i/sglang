name: PR Test For Innersource (Ascend NPU)

on:
  push:
    branches: [ upstream/251031 ]
    paths:
      - "python/**"
      - "scripts/**"
      # - "test/**"
      - ".github/workflows/pr-test-npu-innersource.yml"
  pull_request:
    branches: [ upstream/251031 ]
    paths:
      - "python/**"
      - "scripts/**"
      # - "test/**"
      - ".github/workflows/pr-test-npu-innersource.yml"
  workflow_dispatch:

concurrency:
  group: pr-test-npu-innersource-${{ github.ref }}
  cancel-in-progress: true

jobs:
  per-commit-1-ascend-npu:
    if: (github.repository == 'Ascend/sglang' || github.event_name == 'pull_request') &&
        github.event.pull_request.draft == false
    runs-on: linux-aarch64-a3-2
    strategy:
      fail-fast: false
      matrix:
        part: [0, 1]
    container:
      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/cann:8.3.rc1-a3-ubuntu22.04-py3.11
    steps:
      - name: Pre-config git access token
        run: |
          # as we use a proxy but checkout@v4 only set basic auth header for github.com
          # so we set the extraheader manually
          TOKEN=`echo -n "x-access-token:${{ secrets.GITHUB_TOKEN}}"|base64`
          git config --global http.https://gh-proxy.test.osinfra.cn/.extraheader "AUTHORIZATION: basic $TOKEN"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          # speed up by using infra cache services
          CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
          sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
          pip config set global.index-url http://${CACHING_URL}/pypi/simple
          pip config set global.trusted-host ${CACHING_URL}

          bash scripts/ci/npu_ci_install_dependency.sh a3
          # copy required file from our daily cache
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          # copy download through proxy
          curl -o /tmp/test.jsonl -L https://gh-proxy.test.osinfra.cn/https://raw.githubusercontent.com/openai/grade-school-math/master/grade_school_math/data/test.jsonl

      - name: Print Log Information
        run: |
          bash scripts/ci/log_print.sh

      - name: Run test
        timeout-minutes: 120
        env:
          SGLANG_USE_MODELSCOPE: true
          SGLANG_IS_IN_CI: true
          HF_ENDPOINT: https://hf-mirror.com
          TORCH_EXTENSIONS_DIR: /tmp/torch_extensions
          STREAMS_PER_DEVICE: 32
        run: |
          cd test/srt
          python3 run_suite.py --suite per-commit-1-ascend-npu --timeout-per-file 3600 --auto-partition-id ${{ matrix.part }} --auto-partition-size 2

  per-commit-2-ascend-npu:
    if: (github.repository == 'Ascend/sglang' || github.event_name == 'pull_request') &&
        github.event.pull_request.draft == false
    runs-on: linux-aarch64-a3-2
    strategy:
      fail-fast: false
      matrix:
        part: [0, 1, 2]
    container:
      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/cann:8.3.rc1-a3-ubuntu22.04-py3.11
    steps:
      - name: Pre-config git access token
        run: |
          # as we use a proxy but checkout@v4 only set basic auth header for github.com
          # so we set the extraheader manually
          TOKEN=`echo -n "x-access-token:${{ secrets.GITHUB_TOKEN}}"|base64`
          git config --global http.https://gh-proxy.test.osinfra.cn/.extraheader "AUTHORIZATION: basic $TOKEN"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          # speed up by using infra cache services
          CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
          sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
          pip config set global.index-url http://${CACHING_URL}/pypi/simple
          pip config set global.trusted-host ${CACHING_URL}

          bash scripts/ci/npu_ci_install_dependency.sh a3
          # copy required file from our daily cache
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          # copy download through proxy
          curl -o /tmp/test.jsonl -L https://gh-proxy.test.osinfra.cn/https://raw.githubusercontent.com/openai/grade-school-math/master/grade_school_math/data/test.jsonl

      - name: Print Log Information
        run: |
          bash scripts/ci/log_print.sh

      - name: Run test
        timeout-minutes: 120
        env:
          SGLANG_USE_MODELSCOPE: true
          SGLANG_IS_IN_CI: true
          HF_ENDPOINT: https://hf-mirror.com
          TORCH_EXTENSIONS_DIR: /tmp/torch_extensions
          STREAMS_PER_DEVICE: 32
        run: |
          cd test/srt
          python3 run_suite.py --suite per-commit-2-ascend-npu --timeout-per-file 3600 --auto-partition-id ${{ matrix.part }} --auto-partition-size 3

  # pr-test-npu-innersource-k8s:
  #   if: ${{ github.repository == 'Ascend/sglang' || github.event_name == 'pull_request' }}
  #   runs-on: linux-amd64-cpu-0
  #   container:
  #     image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/cann:8.3.rc1-a3-ubuntu22.04-py3.11
  #   steps:
  #     - name: Pre-config git access token
  #       run: |
  #         # as we use a proxy but checkout@v4 only set basic auth header for github.com
  #         # so we set the extraheader manually
  #         TOKEN=`echo -n "x-access-token:${{ secrets.GITHUB_TOKEN }}"|base64`
  #         git config --global http.https://gh-proxy.test.osinfra.cn/.extraheader "AUTHORIZATION: basic $TOKEN"
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     - name: Run test
  #       timeout-minutes: 600
  #       env:
  #         SGLANG_USE_MODELSCOPE: true
  #         SGLANG_IS_IN_CI: true
  #         HF_ENDPOINT: https://hf-mirror.com
  #       run: |
  #         pip3 install -i https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple kubernetes
  #         cp /data/.cache/kubectl /usr/local/sbin/
  #         export KUBECONFIG=/data/.cache/kb.yaml
  #         cd test/srt/ascend_k8s
  #         while true; do
  #           kubectl delete -f deepep.yaml --ignore-not-found=true || true
  #           if kubectl get po -A | grep -q "mindx-dls-test-sglang"; then
  #             kubectl delete -f deepep.yaml --ignore-not-found=true || true
  #             echo "Found exist sglang job, sleeping for 30 seconds..."
  #             sleep 30
  #           else
  #             echo "No exist sglang job, start deepep case..."
  #             break
  #           fi
  #         done
  #         rm -rf /data/.cache/sglang/
  #         cp -r /__w/sglang/sglang /data/.cache/
  #         python3 -u run_ascend_ci.py
  #         kubectl delete -f deepep.yaml --ignore-not-found=true || true

  pr-test-npu-finish:
    if: always()
    needs:
      - per-commit-1-ascend-npu
      - per-commit-2-ascend-npu
      # - pr-test-npu-innersource-k8s
    runs-on: linux-amd64-cpu-1
    container:
      image: docker.m.daocloud.io/ubuntu:22.04
    steps:
      - name: Check all dependent job statuses
        run: |
          #!/bin/sh
          results="${{ join(needs.*.result, ' ') }}"
          set -- $results
          for result in "$@"; do
            if [ "$result" = "failure" ] || [ "$result" = "cancelled" ]; then
              echo "Job failed with result: $result"
              exit 1
            fi
          done
          echo "All jobs completed successfully"
          exit 0
